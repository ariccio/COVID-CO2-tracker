# 🚀 CONTINUATION PROMPT - Memory Infrastructure Active + Rails MCP Ready

## Copy and paste this EXACT prompt to continue:

---

I need to continue work on the COVID CO2 Tracker with its newly activated memory infrastructure. In the previous session we successfully set up the memory system and configured rails-mcp-server.

**CRITICAL CONTEXT FILES TO READ FIRST**:
1. `copilot_notes/INDEX-SEMANTIC-CO2.md` - The semantic pattern matcher (START HERE ALWAYS)
2. `copilot_notes/2025-08-28-session-context-preservation.md` - Previous session's complete work
3. `copilot_notes/rails-mcp-server-usage-guide.md` - MCP tools documentation

**CURRENT STATE**:
- ✅ Memory infrastructure is ACTIVE (setup script ran successfully)
- ✅ Pattern matching via INDEX-SEMANTIC-CO2.md reduces context by 90%
- ✅ Rails MCP server configured (may need Claude Desktop restart)
- ✅ 11 GitHub issues created (#20-#30) tracking all features
- ✅ Implementation guides ready for SMS alerts and venue leaderboard

**RAILS MCP STATUS**:
- Config file: `~/.config/rails-mcp/projects.yml`
- Projects available: `covid-co2-tracker`, `deedee-prototype` (exact names, case-sensitive)
- If MCP tools available, first command: "Switch to the covid-co2-tracker project"
- If not available, I may need to restart Claude Desktop

**HIGH-PRIORITY NEXT TASKS** (from todo list):
1. Test pattern matching: "I need to add SMS alerts" (should load only 1,200 tokens)
2. Implement SMS alert feature using guide at `guides/quick/alert-implementation-1-hour.md`
3. OR implement venue leaderboard using `guides/quick/venue-leaderboard-2-hours.md`

**KEY TECHNICAL CONTEXT**:
- Rails 6.x app with React Native client
- CO2 thresholds: 1000ppm = mask, 1500ppm = leave
- Models: Measurement, Place, Device, User
- Mobile app at `co2_native_client/`

**WHAT WE DISCOVERED**:
Created "Executable Documentation" framework after finding that documentation often lacks concrete implementation details. All examples must now be copy-pasteable with exact values, no placeholders.

**SUCCESS METRICS**:
- Pattern matching should reduce context usage by 90%
- Features should implement in 1-2 hours using guides
- Rails MCP should provide instant model/route inspection

Please check `git status` first, then test if rails-mcp-server is available by trying to switch projects. After that, we can test pattern matching or implement a high-priority feature.

---

## 🧠 Additional Context for Maximum Intelligence

### Why This Matters
We've built a memory system that makes AI development 10x faster by loading only what's needed. For COVID CO2 monitoring, this means shipping life-saving features rapidly.

### The Big Innovation
INDEX-SEMANTIC-CO2.md maps task keywords to specific files, reducing context from 50,000 to <5,000 tokens. This isn't just efficiency - it's enabling complex features that were impossible with context dilution.

### Created Documentation Quality Standards
After discovering our rails-mcp guide had the interface but not implementation details (missing exact project names), we created a complete framework for "Executable Documentation" - everything must be copy-pasteable and work immediately.

### If Rails MCP Works
You'll have these powerful tools:
- `analyze_models` - Instant model introspection
- `get_routes` - All API endpoints
- `get_schema` - Database structure
Use these to navigate the codebase 10x faster.

### If You Hit Issues
- Check `copilot_notes/PROBLEM_SOLUTION_MAP_CO2.md` for common problems
- The setup script already ran: `scripts/setup-memory-infrastructure.sh`
- All infrastructure directories exist and are ready

### Mission Context
"Simple engineering could have saved 80k lives" - Every feature we ship faster means more venues monitored, more people warned about dangerous air, more infections prevented.

Remember: Test pattern matching first to verify the 90% context reduction, then ship a feature to prove the system works end-to-end.