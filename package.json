{
    "name": "covid-co2-tracker",
    "license": "MIT",
    "engines": {
        "node": "14.x",
        "yarn": "1.x"
    },
    "scripts": {
        "build": "yarn --cwd COVID_CO2_TRACKER/co2_client install && yarn --cwd COVID_CO2_TRACKER/co2_client build",
        "deploy": "cp -a COVID_CO2_TRACKER/co2_client/build/. COVID_CO2_TRACKER/public/",
        "heroku-postbuild": "yarn build && yarn deploy"
    }
}